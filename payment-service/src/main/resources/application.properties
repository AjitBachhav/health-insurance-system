# Application specific properties (can be overridden by Config Server)

# Server Port (Consider moving to Config Server for flexibility)
# server.port=8084

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Default Datasource configuration (SHOULD BE MOVED TO CONFIG SERVER for security)
# spring.datasource.url=jdbc:postgresql://localhost:5432/payment_db
# spring.datasource.username=payment_service_user
# spring.datasource.password=secret
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
# spring.jpa.hibernate.ddl-auto=update # Use 'validate' or 'none' in production
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging Configuration
logging.level.com.insurance.paymentservice=INFO
logging.level.org.springframework.web=INFO

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics,prometheus

# Payment Gateway Configuration (MOVE TO CONFIG SERVER)
# stripe.api.key=sk_test_YOUR_SECRET_KEY
# stripe.webhook.secret=whsec_YOUR_WEBHOOK_SECRET

# Event messaging properties (if listening to PolicyCreatedEvent)
# spring.kafka.bootstrap-servers=localhost:9092
# spring.kafka.consumer.group-id=payment-group
# spring.rabbitmq.host=localhost

